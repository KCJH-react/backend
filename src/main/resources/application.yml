spring:
  config:
    import: optional:file:.env[.properties]
  datasource:
    url: jdbc:mysql://localhost:3306/challenge
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # create

    show-sql: true
    properties:
      hibernate:
        hbm2ddl:
          defer_foreign_key_constraints: true
        dialect: org.hibernate.dialect.MySQL8Dialect  # ??? ??
        format_sql: true
    defer-datasource-initialization: true
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 5000ms
  mail:
    host: smtp.gmail.com
    port: 587
    username: verify0213@gmail.com
    password: ceip atbk wtff yufc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server.port: 8020
springdoc:
  swagger-ui:
    enabled: true
    operations-sorter: method
#token: # ??? ????
#  secret-key: "SECRET"
#  refresh-secret-key: "REFRESH_SECRET"
#  token-time: 300
#  refresh-token-time: 500

firebase:
  bucketName: imgstorage-6ba5c.firebasestorage.app
openai:
  model: ${MODEL}
  secret-key: ${GPT}

logging:
  level:
    org.hibernate.SQL: debug         # 쿼리 출력
    org.hibernate.orm.jdbc.bind: trace   # 바인딩된 값 출력